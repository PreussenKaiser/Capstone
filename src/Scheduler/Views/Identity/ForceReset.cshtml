@model SecurityViewModel

<div class="row">
    <div class="col-sm-6 mx-auto">
        <form asp-action="ForceReset">
            <div class="text-danger">You must reset your password before using the app.</div>

            @await this.Html.PartialAsync("Forms/_ResetPasswordInputs", this.Model)

            <button class="buttonRed w-100">
                Reset Password
            </button>
        </form>
    </div>
</div>
