@model IEnumerable<Field>

@{
    this.ViewData["Title"] = "Fields";
}

<div class="text-center mb-3 mt-3">
    <div class="linkElement">
        <a asp-action="Add" asp-controller="Field" class="mainButton">
            Add New Field
        </a>
    </div>
</div>

<div class="row m-auto">
    @foreach (var field in this.Model)
    {
        <div class="col-lg-3 mb-3 field-card">
            <div class="card border border-3 border-success">
                <img src="~/Images/field.png" class="card-img-top rounded-0">
                <div class="card-body">
                    <h5 class="text-center mb-3">@field.Name</h5>
                    <div class="row">
                        <div class="col-sm-6 mb-3 mb-sm-0">
                            <form asp-action="Details" asp-controller="Field" asp-route-id="@field.Id" method="get">
                                <button type="submit" class="button buttonRedOutline w-100">
                                    Edit
                                </button>
                            </form>
                        </div>
                        <div class="col-sm-6">
                            <div class="linkElement">
                                <button type="button" class="button buttonRed w-100" data-bs-toggle="modal" data-bs-target="#remove@(field.Id)">
                                    Remove
                                </button>
                            </div>
                            <div class="modal fade" id="remove@(field.Id)" tabindex="-1" aria-hidden="true">
                                <div class="modal-dialog">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h5 class="modal-title">@field.Name</h5>
                                        </div>
                                        <div class="modal-body">
                                            Are you sure you want to remove @(field.Name)?
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" class="button buttonRedOutline" data-bs-dismiss="modal">Close</button>
                                            <form asp-action="Remove"
                                                  asp-controller="Field"
                                                  asp-route-id="@field.Id"
                                                  method="post">
                                                <input type="submit" value="Remove" class="button buttonRed" />
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
</div>